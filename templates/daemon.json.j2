{
  "registry-mirrors": ["{{ reg_mirror_1 }}", "{{ reg_mirror_2 }}", "{{ reg_mirror_3 }}"], 
  "max-concurrent-downloads": 10,
  "log-driver": "{{ log_driver }}",
  "log-level": "{{ log_level }}",
  "log-opts": {
    "max-size": "{{ log_max_size }}",
    "max-file": "{{ log_max_file }}"
    },
  "graph": "{{ docker_data_dir }}"
}
